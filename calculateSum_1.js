/**
 * @Studio
 * @property {number} studio_firstNumber - Primeiro número da soma
 * @property {number} studio_lastNumber - Último número da soma
 */
function calculateSum() {
    let sum = 0;
    sum = studio_firstNumber + studio_lastNumber;
    return sum;
}
